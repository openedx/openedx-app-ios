strings:
  inputs:
    - Discussion/en.lproj
  # The filter below matches .strings or .stringsdict files
  # BUT excludes any file named "CustomLocalizable.strings"
  filter: '^(?!.*CustomLocalizable\.strings$).*\.(?i:strings|stringsdict)$'
  outputs:
    - templateName: structured-swift5
      params:
        publicAccess: true
        enumName: DiscussionLocalization
      output: Discussion/SwiftGen/Strings.swift
#xcassets:
#  inputs:
#    - Authorization/Assets.xcassets
#  outputs:
#    templateName: swift5
#    params:
#        publicAccess: true
#        enumName: AuthAssets
#    output: Authorization/SwiftGen/Assets.swift
